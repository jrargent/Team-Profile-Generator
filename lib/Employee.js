class Employee {
    constructor(name, number, email) {
    // will need to have these arguments be user inputs from inquirer prompt
        this.name = name;
        this.id = number;
        this.email = email;
    }

// getName() {
// get name from inquirer prompt? put into HTML
// };

// getId() {
    //get ID from inquirer prompt? put into HTML
// };

// getEmail() {
    //get email from inquirer prompt? put into HTML
// };

}


module.exports = Employee;